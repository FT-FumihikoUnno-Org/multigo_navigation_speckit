{
  "name": "webapp",
  "private": true,
  "workspaces": [
    "src/frontend",
    "src/backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend & npm run dev --workspace=backend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "e2e:install": "npx playwright install",
    "e2e:test": "playwright test --reporter=html,junit",
    "e2e:open": "playwright test --ui",
    "e2e:setup": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build && npm run migrate --workspace=backend"
  },
  "dependencies": {
    "whatwg-url": "^11.0.0"
  },
  "devDependencies": {
    "playwright": "^1.40.0",
    "@playwright/test": "^1.40.0",
    "pg": "^8.10.0"
  }
}
